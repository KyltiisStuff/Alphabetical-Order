with open("word list.txt") as f:
  words = [line.strip() for line in f]
letters = []
string = ""
for word in words:
  letters = []
  for letter in word:
    letters.append(letter)
  letters.sort()
  string = "".join(letters)
  print(string)
